[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auditchain"
version = "0.1.0"
description = "An append-only hash-chained audit log with verifiable prefix pruning"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "cryptography",
]

[tool.hatch.build.targets.wheel]
packages = ["auditchain"]
